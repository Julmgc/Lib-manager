import { Router } from "express";
// import { getUsers } from "../controllers/userController";

const userRouter = () => {
  const router = Router();

  //   router.get("", getUsers);
  return router;
};

export default userRouter;
